import { User } from "./models/User";

const user = new User({ name: 'name', age: 2 });

user.on('change', () => {

})

user.on('change', () => {

})

user.on('changer', () => {

})

console.log(user)